# @agent-runtime/daemon

TypeScript background-agent daemon: detached worker management, `agent agents` dashboard backend, `/loop` and `/schedule` executor, warm-spare pool, idle retirement.

Phase 0 status: scaffolded, no implementation. See `docs/PLAN.md`.
