# agent-runtime-mcp-adapters

Python MCP client adapters: stdio, SSE, HTTP, WebSocket. OAuth flows (DCR, CIMD, RFC 9728 PRM, step-up). Exposed to TS core via JSON-RPC over stdio.

Phase 2 will port the `_base.py` abstraction from `~/Code/_agent-ontology/` (6 production adapters shipped 2026-05-17). Phase 0 status: scaffolded, no implementation.
