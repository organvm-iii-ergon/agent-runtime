# @agent-runtime/core

TypeScript runtime: plugin loader, hook dispatcher, skill loader, slash command registry, permission rules, session manager, context compaction.

Current status: initial slash-command registry and suggestion engine implemented, plus OpenCode markdown-command discovery from `~/.config/opencode/commands/` and `.opencode/commands/`. Broader runtime primitives are still scaffolded. See `docs/PLAN.md`.
